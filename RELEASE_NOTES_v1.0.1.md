## Nuzi Inventory v1.0.1

### Fixes
- Cleaned up the inventory window layout so the search row, alt snapshot row, and tracked bar controls no longer crowd each other.
- Fixed the `Icons / Row` controls so the value reads clearly as `Icons / Row: #` instead of bleeding into the minus button.
- Reworked the footer pager so the arrows sit on the ends with the page count centered between them.

### New
- Widened and rebalanced the window layout for better spacing across the header, list columns, and footer controls.
